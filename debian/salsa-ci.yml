---
# Include Salsa-CI as a base
include:
  - https://salsa.debian.org/salsa-ci-team/pipeline/-/raw/master/recipes/ubuntu.yml

variables:
  # These pass in Ubuntu 24.04 "Noble", to continue testing that they don't regress
  SALSA_CI_DISABLE_LINTIAN: 0
  # Testing https://salsa.debian.org/salsa-ci-team/pipeline#git-attributes
  SALSA_CI_DISABLE_GBP_SETUP_GITATTRIBUTES: 1

.build-package:
  # Extend Salsa CI build jobs to have longer timeout as the default GitLab
  # timeout (1h) is often not enough
  timeout: 3h
  # Default 5G sporadically fails builds on not having enough disk space
  variables:
    CCACHE_MAXSIZE: 3G
  # Salsa instance runners typically have 30G volumes with 14G free disk space
  before_script:
    - echo "Total and free disk space:"
    - df -h .
